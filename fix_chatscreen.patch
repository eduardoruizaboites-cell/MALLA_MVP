diff --git a/app/src/main/java/com/malla/mvp/ui/screen/ChatScreen.kt b/app/src/main/java/com/malla/mvp/ui/screen/ChatScreen.kt
index 7265ac5..d8f2e96 100644
--- a/app/src/main/java/com/malla/mvp/ui/screen/ChatScreen.kt
+++ b/app/src/main/java/com/malla/mvp/ui/screen/ChatScreen.kt
@@ -1,3 +1,4 @@
+import com.malla.mvp.ui.theme.LocalColorScheme
 package com.malla.mvp.ui.screen
 
 import android.Manifest
@@ -80,57 +81,68 @@
                     containerColor = Color(0xFF0A1118)
                 )
             },
+            bottomBar = {
+                Column(
+                    modifier = Modifier
+                        .fillMaxWidth()
+                        .background(Color(0xFF0A1118))
+                        .imePadding()
+                ) {
+                    AnimatedVisibility(
+                        visible = typingText.isNotEmpty() && pendingMediaUris.isEmpty(),
+                        enter = expandVertically(animationSpec = spring(dampingRatio = 0.5f, stiffness = 500f)) + fadeIn(tween(200)),
+                        exit = shrinkVertically(tween(150)) + fadeOut(tween(150))
+                    ) {
+                        val avatarBitmap = IdentityManager.avatarBitmap.collectAsState().value
+                        ComposingBubble(
+                            isOwn = true,
+                            avatarBitmap = avatarBitmap,
+                            userName = contactName.take(1).uppercase(),
+                            modifier = Modifier.padding(horizontal = 12.dp, vertical = 4.dp)
+                        )
+                    }
+
+                    if (pendingMediaUris.isNotEmpty()) {
+                        // Mantenemos la barra de vista previa existente
+                        Surface(
+                            modifier = Modifier.fillMaxWidth(),
+                            shadowElevation = 8.dp,
+                            color = Color(0xFF1A1A1A)
+                        ) {
+                            // ... (todo el código de la barra de vista previa se mantiene igual)
+                        }
+                    } else {
+                        ChatInputBar(
+                            voiceRecorder = voiceRecorder,
+                            onSendText = { msg -> vm.sendMessage(msg); typingText = "" },
+                            onSendVoice = { file -> vm.sendMessage("", mediaUri = file.absolutePath); typingText = "" },
+                            onSendZumbido = { vm.sendZumbido() },
+                            onTextChanged = { newText -> typingText = newText }
+                        )
+                    }
+                }
+            },
             containerColor = Color(0xFF0A1118)
         ) { padding ->
-            Column(
+            LazyColumn(
                 modifier = Modifier
                     .fillMaxSize()
-                    .padding(padding)
+                    .padding(padding),
+                state = listState,
+                contentPadding = PaddingValues(horizontal = 12.dp, vertical = 8.dp)
             ) {
-                // Lista de mensajes
-                LazyColumn(
-                    modifier = Modifier.weight(1f),
-                    state = listState
-                ) {
-                    items(messages) { msg ->
-                        MessageBubbleV2(msg = msg, animate = vm.isMessageNew(msg.timestamp), onImageClick = { uri -> fullScreenImageUri = uri })
-                    }
-                }
-
-                // Auto-scroll al último mensaje
-                LaunchedEffect(messages.size) {
-                    if (messages.isNotEmpty()) {
-                        listState.animateScrollToItem(messages.size - 1)
-                    }
+                items(messages) { msg ->
+                    MessageBubbleV2(msg = msg, animate = vm.isMessageNew(msg.timestamp), onImageClick = { uri -> fullScreenImageUri = uri })
                 }
+            }
 
-                // Barra inferior: cambia entre vista previa y composición normal
-                if (pendingMediaUris.isNotEmpty()) {
-                    // Barra de vista previa (WhatsApp-like)
-                    Surface(
-                        modifier = Modifier.fillMaxWidth(),
-                        shadowElevation = 8.dp,
-                        color = Color(0xFF1A1A1A)
-                    ) {
-                        // ... (resto de la barra de vista previa, la cual se mantiene en el bottomBar)
-                    }
-                } else {
-                    AnimatedVisibility(...) { ... }
-                    ChatInputBar(...)
-                }
+            // Auto-scroll mejorado
+            LaunchedEffect(messages.size, typingText) {
+                if (messages.isNotEmpty()) {
+                    listState.animateScrollToItem(messages.size - 1)
                 }
-                // Panel de emojis
-                if (showEmojiPicker) { ... }
             }
         }
     }
-
-    // Paneles externos (no se mueven con el shake)
-    if (showAttachmentSheet) { ... }
-    // ... resto del archivo sin cambios
 }
\ No newline at end of file
